<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <declare-styleable name="estilos">
        <attr name="colorCirculo" format="color"/>
        <attr name="etiquetaCirculo" format="string"></attr>
        <attr name="colorEtiqueta" format="color"></attr>
    </declare-styleable>

</resources>
